# Substitua seu-dominio.com pelo seu domínio real
seu-dominio.com {
  encode zstd gzip

  @api path /chatbot* /os* /api*
  reverse_proxy @api api:3000

  # Demais rotas servem o frontend
  reverse_proxy web:80
}
