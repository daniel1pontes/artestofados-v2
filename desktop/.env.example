# Desktop (React) environment variables
# Deixe vazio em produção para usar same-origin via Nginx (recomendado)
# Em desenvolvimento local, defina para o backend local
# Exemplo DEV:
# REACT_APP_API_URL=http://localhost:3000
REACT_APP_API_URL=
